# Neko Status Desktop 开发规范与工作流

> 适用范围：当前 Electron 桌面端仓库
> 最后更新：2026-05-17

## 1. 文档定位

这份文档是团队开发入口，回答四件事：

- 代码应该放哪里
- 日常开发怎么启动和验证
- 功能改动怎么穿过 IPC、配置和页面边界
- PR、发布和文档更新如何配套

专题细节请看：

- [architecture.md](/D:/VScode%20project/Neko_Status/docs/architecture.md)
- [ipc-contract.md](/D:/VScode%20project/Neko_Status/docs/ipc-contract.md)
- [frontend-guideline.md](/D:/VScode%20project/Neko_Status/docs/frontend-guideline.md)
- [testing-guideline.md](/D:/VScode%20project/Neko_Status/docs/testing-guideline.md)
- [feature-lifecycle.md](/D:/VScode%20project/Neko_Status/docs/feature-lifecycle.md)
- [RELEASE_WORKFLOW.md](/D:/VScode%20project/Neko_Status/docs/RELEASE_WORKFLOW.md)

## 2. 当前工程结构

```text
src/
  main/
    main.js
    app-shell.js
    config-store.js
    config-store.helpers.js
    api-service.js
    status-service.js
    stream-service.js
    system-utils.js
    ipc/
      index.js
      api.ipc.js
      auth.ipc.js
      config.ipc.js
      service.ipc.js
      stream.ipc.js
      system.ipc.js
      update.ipc.js
  preload/
    index.js
    privacy-picker.js
  renderer/
    index.html
    css/
      main.css
    js/
      app.js
      app-ipc.js
      ipc-bridge.js
  shared/
    ipc-contracts.js
    schemas.js

tests/
  unit/
```

当前处于“工程化迁移期”：

- 主进程由 `main.js` 负责启动编排，窗口/托盘/隐私选择器已拆到 `app-shell.js`，所有 IPC handler 已拆到 `src/main/ipc/*.ipc.js`
- `main.js` 中不再保留任何内联 IPC handler
- preload 已成为 renderer 调用 Electron 能力的默认入口
- 新功能优先落在新边界，不再继续扩大 renderer 直接访问 Electron 的旧模式

## 3. 日常开发命令

```bash
npm start
npm run lint
npm test
npm run verify
npm run build
npm run build:zip
```

约定：

- `npm start`：本地开发启动
- `npm run lint`：当前等价于项目自定义结构校验脚本
- `npm test`：最小单元测试
- `npm run verify`：提交前必须通过，等价于 `lint + test`
- `npm run build`：生成安装包
- `npm run build:zip`：生成 ZIP 发布产物

条件型 UIUX 的开发版场景命令：

```bash
npm run dev:startup-update:checking
npm run dev:startup-update:available
npm run dev:startup-update:download
npm run dev:startup-update:failed
npm run dev:startup-update:offline
```

这些命令用于复现启动前自动更新窗口的检查、发现更新、下载进度、失败放行和离线放行状态。新增只能在特定条件下出现的 UI 时，必须同步提供类似的开发版触发方式，避免后续只能靠真实网络、真实发布包或临时改代码验证。

## 4. 开发边界

### 主进程

- `src/main/main.js` 负责启动编排、生命周期、自动更新调度和版本比较工具
- `src/main/app-shell.js` 负责窗口、托盘、初始状态同步与隐私窗口选择器
- `src/main/ipc/*.ipc.js` 负责各领域 IPC handler；新增主进程 IPC 时优先放到这里
- 与系统交互的纯能力尽量放进 `system-utils.js`
- 配置默认值和兼容逻辑统一维护在 `config-store.helpers.js`
- 复杂 IPC payload 优先走 `src/shared/schemas.js`

### Preload

- Electron 能力只通过 preload 暴露给 renderer
- `src/preload/index.js` 是主页面入口
- `src/preload/privacy-picker.js` 只给隐私选择器使用
- renderer 不再新增 `require('electron')` 直连实现

### Renderer

- `app.js` 负责视图层与交互
- `app-ipc.js` 负责 IPC 数据接线
- 新增页面逻辑时，优先按 `pages / services / state / components` 方向拆分

### Shared

- `src/shared/ipc-contracts.js` 维护 invoke channel 与 event 常量
- `src/shared/schemas.js` 维护跨层 payload 校验

## 5. 验证要求

提交前至少完成：

1. `npm run verify`
2. 与改动相关的手工 smoke test
3. 如改动涉及打包、更新、托盘、截图、推流等系统行为，再额外跑一次 `npm run build` 或 `npm run build:zip`

## 6. CI 约束

- `ci.yml`：运行 `npm run verify`
- `build-check.yml`：Windows 环境运行 `npm run verify` 与 `npm run build:zip`
- `release.yml`：发布前同样执行 `npm run verify`

## 7. 功能改动原则

- 改功能前先判断是否需要新增 IPC
- 改配置前先补默认值与兼容迁移
- 改 renderer 前先判断是否应该抽成页面、状态或服务
- 改更新逻辑前同步检查 [RELEASE_WORKFLOW.md](/D:/VScode%20project/Neko_Status/docs/RELEASE_WORKFLOW.md)
- 改 IPC 时同步检查 [ipc-contract.md](/D:/VScode%20project/Neko_Status/docs/ipc-contract.md)

## 8. 文档同步要求

出现以下情况时必须更新文档：

- 新增或废弃 IPC
- 新增 preload 能力
- 新增配置项
- 修改窗口、托盘、更新、截图、推流等关键系统行为
- 修改开发命令、测试命令或打包命令

优先同步：

- 本文
- 对应专题文档
- `CONTRIBUTING.md`

## 9. 当前继续推进方向

- ✅ 主进程 IPC 拆分已全部完成（config / api / auth / stream / system / service / update）
- 把 renderer 从 `app.js / app-ipc.js` 逐步拆成更清晰的页面与服务结构
- 推进更多主进程返回值使用统一结果结构（`createIpcSuccess` / `createIpcError`）
- 扩展 shared 与 main 层测试覆盖

在迁移完成前，所有新改动都应朝这个方向靠拢，而不是继续扩大旧的耦合实现。
